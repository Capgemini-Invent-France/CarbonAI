image: python:3.7

pages:
  stage: deploy
  script:
  - pip install -U sphinx sphinx-rtd-theme pandas IPython psutil
  - sphinx-build -b html docs/source public

  artifacts:
    paths:
    - public
  only:
  - master
